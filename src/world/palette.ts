// 전장 팔레트 · 조명 — **서로 얽혀 있어 함께 튜닝해야 하는 값들**을 한곳에 모은다.
//
// 왜 한 파일인가: 이 값들은 독립적이지 않다. 하늘을 어둡게 하면 청백 플라즈모이드가 살아나지만
// 바다와 붙고, 태양을 낮추면 그늘이 죽고, 랜드마크 색을 정하면 황토 지표와 충돌한다.
// 흩어 두면 하나를 고칠 때 다른 것이 조용히 무너진다(실제로 그랬다 — 2026-08-23 검토).
// tests/palette.test.ts 가 **요소 간 최소 분리**를 계약으로 고정한다.
//
// ── 렌더 파이프라인(수치를 읽을 때 전제) ──
//   씬 → 렌더타깃(**선형 HDR, 톤매핑 없음**) → 블룸(선형 기준 임계 0.75) → OutputPass(ACES + sRGB)
//   three.js 는 렌더타깃에 그릴 때 머티리얼 톤매핑을 끈다(`toneMapping = NoToneMapping`).
//   확산 반사 = albedo × irradiance / π — 조명 강도를 그대로 곱하면 3배쯤 과대평가된다.
//
// ── 우선순위(2026-08-23 확정) ──
//   ① 게임 정보를 나르는 요소(적·빔·랜드마크·잔해)가 배경에 묻히지 않을 것
//   ② 그 다음이 "너무 비현실적이지 않을 것" — 양자장 플라즈모이드 세계관이라 사실주의는 후순위
//   배경끼리 붙는 것(눈↔건물, 황토↔담장)은 정보 손실이 아니므로 계약에서 제외한다.

import * as THREE from "three";

/**
 * 하늘(scene.background) — **거의 무채색 회색**. 파랑을 비워 물에게 내준다(물이 청록으로 밀려나
 * 초록 지형과 같은 계열로 읽히던 문제). 채도 0.97(구 0x2f9bf2) → 0.10 으로 톤이 크게 차분해진다.
 * 밝기가 아니라 **채도**가 차분함을 만든다.
 *
 * ⚠️ **무채색 하늘은 밝기를 아무 데나 둘 수 없다** — 건물도 무채색이라 그 스펙트럼
 * (그늘 0.280 ~ 직사광 0.723) 어딘가와 반드시 부딪힌다. 실측:
 *   하늘 0.20~0.34 → 건물(그늘)과 분리 **0.088** (실루엣이 하늘에 묻힘)
 *   하늘 0.609     → 건물(직사광)과 분리 **0.122**
 *   하늘 0.498     → 양쪽 0.240 / 0.242 ✓  ← **두 상태의 중간**이 유일한 안전지대
 * 어둡게 가려면 0.08 미만(거의 검정)이어야 하는데 낮 전장이 아니다.
 *
 * ⚠️ **하늘 밝기를 바꾸면 HUD 가독성을 반드시 재확인할 것.** HUD 는 DOM 오버레이라 이 팔레트
 * 계약 밖에 있지만 배경은 하늘이다 — 실제로 여기를 밝게 하자 반투명 시안 라벨이 씻겨 나갔다.
 * 대응은 styles.css `.hud` 의 상속 text-shadow(+ --hud-dim 알파 상향).
 */
export const SKY_COLOR = 0xa1a5a9;

/**
 * 포그 — **청크 경계 은폐가 실질 기능**이다(로드 반경 밖은 지오메트리가 없어 도시가 잘려 보인다).
 * 그래서 교전 사거리를 흐리지 않도록 늦게 시작해 로드 반경에서 끝난다. 색은 하늘과 같아
 * 원경이 하늘로 녹는다. 적(디졸브 셰이더)은 포그 처리가 없어 영향받지 않는다.
 *
 * far 는 StreamingWorld 의 청크 로드 반경(`fineRadius`)과 **맞물려야** 한다 — 생성자 인자로 받는다.
 */
export const FOG_NEAR_RATIO = 0.69; // far 대비 시작 지점(2600m 기준 ≈1800m — 그 안은 완전 선명)
export const FOG_FAR_DEFAULT = 2600; // 스트리밍 로드 반경 기본값(모놀리식 World 폴백)

/**
 * 포그 색 — **하늘색보다 밝게**. 하늘색과 같게 두면 포그가 지형보다 어두워 원경이 *가라앉는다*
 * (대기 원근은 통상 먼 것을 밝게 만드는데 정반대가 된다 — 실측: 원경 휘도 0.221 vs 근거리 물 0.426).
 * 지금은 0.438 로 근거리 물(0.426)과 거의 같아 거리에 따른 왜곡이 사라진다.
 *
 * 하늘과의 차이는 수평선 헤이즈로 읽힌다 — 실제 하늘도 지평선 쪽이 밝다. 경계 은폐는
 * "지형이 균일한 색으로 수렴"하는 것이 핵심이라 하늘과 정확히 같을 필요는 없다.
 * 원경 적의 배경이기도 하므로 시인성 계약 대상이다(적청백 0.277 · 빔 0.316).
 */
export const FOG_COLOR = 0xa9a9ac;

/** 조명 — 차분한 톤 = 전체 감광이 아니라 **태양↓ + 반구광 유지**(흐린 날 구조). 그늘이 죽지 않는다. */
export const LIGHT = {
  hemiSky: 0xbfdcff,
  hemiGround: 0x6f7a4a,
  hemi: 1.3, //  ↑ 1.18 — 태양을 낮춘 만큼 올려 그늘 휘도를 보존(0.281 유지)
  sunColor: 0xfff3da,
  sun: 1.0, //   ↓ 2.00 — 직사광 억제(명암 완화 + 건물 블룸 감소)
  fillColor: 0xaecbe6,
  fill: 0.35,
} as const;

/** 톤매핑 노출(renderer.toneMappingExposure) — 팔레트 계산의 전제라 여기 둔다. */
export const EXPOSURE = 0.95;

/**
 * 수역 — **화면에 보이는 물 색**(지형 표면 텍스처에 베이크, chunkMesh.WATER_CSS).
 * 강·호수(OSM 폴리곤 경로)와 바다(표고 ≤0 경로)가 **같은 색**을 쓴다 — 아래 참조.
 *
 * **확실한 파랑**(B 180 > G 104). 이전 0x4c7072 는 G 112·B 114 로 사실상 청록이어서, 어두워지자
 * 초록 지형과 같은 계열로 읽혔다. 하늘에서 파랑을 비운 덕에 물이 제 색을 가질 수 있게 됐다.
 *
 * **하늘보다 어둡다**(0.132 vs 0.498). 물은 하늘을 반사하되 흡수가 있어 더 어두운 게 정상이고,
 * 밝았을 때(0.426)는 물이 발광체처럼 보였다.
 * ⚠️ 청색 하늘 시절엔 "하늘 휘도 바로 아래"가 함정이었다(분리 0.065) — 지금은 하늘이 밝은 회색이라
 * 그 함정에서 멀지만, 물을 하늘 쪽으로 올릴 땐 다시 확인할 것.
 * 파랑 우세(B>G)는 계약이다 — 초록으로 기울면 지형과 붙고, geo.test 도 이를 요구한다.
 */
export const WATER_COLOR = 0x2f68b4;

// 표고 0m 이하 지형(geo.elevationColor)도 **같은 WATER_COLOR** 를 쓴다 — 상수를 나누지 않는다.
//
// 왜 합쳤나: OSM 은 바다를 폴리곤으로 주지 않아(coastline 태그뿐) 바다는 표고 경로로, 강·호수는
// 폴리곤 경로로 그려진다. 색을 따로 두면 같은 화면에서 두 종류 물이 딴판으로 보이는데, 그 차이는
// 설계 의도가 아니라 구현 부산물이다 — 게임 정보가 아닌 차이가 정보처럼 읽힌다.
// 게다가 두 상수를 짝으로 유지하는 건 실제로 실패했다(2026-08-23: 화면의 물로 착각해 지형 쪽만
// 어둡게 바꿔 강변이 검게 변했다). 하나면 그 혼동이 구조적으로 불가능하다.

/** 담장/울타리 — 이전 0x9c948a 는 어두워진 그늘 건물과 붙었다(분리 0.092). 밝혀서 분리. */
export const WALL_COLOR = 0xc4b9a8;

/** 잔해 더미 — 순수 검정이면 어두운 배경에서 형태가 사라진다. 아주 어두운 청회색으로 최소 대비 확보. */
export const RUBBLE_COLOR = 0x151a1e;

/**
 * 랜드마크 정점색(**linear 직접 지정** — 알베도가 1 을 넘어 어떤 일반 건물보다 밝다).
 * 호박색인 이유: 금색(1.5,1.1,0.42)은 황토 지표(해변·바위·포장)와 0.120 으로 겹쳤다.
 * 자홍·청자도 계산했으나 플라즈모이드 스펙트럼과 부딪혀 더 나빴다(0.140/0.110).
 * 미니맵 랜드마크(C_LANDMARK)와 같은 색 언어. FLASH_COLOR(2.0)보다 어두워 파괴 번쩍임이 우선한다.
 */
export const LANDMARK_COLOR = new THREE.Color(1.6, 0.92, 0.22);
